
const isInsideGrid = (( position, gridDimensions ) => {  
  if ( exp ) {
  return true;
} else {
  throw new Error("The grid size must be between 0 and 50 in both axis");
}});


module.exports = {
  isInsideGrid: isInsideGrid,
};
